# 开发者指南

本文档面向参与内容运营系统开发的工程师，涵盖环境搭建、项目结构、编码规范、测试、构建和常见问题处理。

> **当前状态（2026-08-09）**：本轮文档与代码对齐以 API、Web、shared、Prisma 和非 EXE 验证为准。P0-03 迁移基线与 P0-04 关键写入幂等已完成；Windows Desktop、EXE、安装器和安装后 smoke 明确延期，相关章节只保留后续发布参考。完整状态见 [文档对齐总览](docs/DOCUMENTATION-STATUS.md)。

## 环境要求

- Node.js `>=20.0.0 <23`
- npm `>=10.0.0`（项目使用 workspaces）
- Windows 10/11 仅是后续桌面发布的目标环境；日常 API/Web 开发不要求执行 EXE 打包
- Git

## 首次搭建

```bash
git clone <repo-url>
cd 内容运营
npm install                    # 安装依赖 + 自动执行 prisma generate
npm run prepare:db             # 初始化 SQLite 表结构（默认创建 prisma/dev.db）
cp .env.example .env           # 复制环境变量模板并按需配置
npm run dev                    # 启动开发服务器
```

`npm run dev` 会同时启动后端（tsx watch，端口 3101）和前端（Vite，端口 3100），前端通过 Vite 代理将 `/api` 请求转发到后端。开发时统一访问 `http://localhost:3100`。

## Monorepo 结构

项目使用 npm workspaces 管理三个包：

```
@content/shared   packages/shared    前后端共享 TypeScript 类型
@content/api      apps/api           NestJS 后端
@content/web      apps/web           Vue 3 前端
```

构建顺序有依赖关系：shared 必须先于 api 和 web。`npm run build` 命令已按正确顺序编排，正常情况下不需要单独构建。

如需单独构建某个包：

```bash
npm run build -w @content/shared
npm run build -w @content/api
npm run build -w @content/web
```

## 后端架构

### 模块结构

后端采用 NestJS 标准模块架构，核心业务集中在 `apps/api/src/content/` 目录下：

| 文件 | 职责 |
|------|------|
| `content.module.ts` | 模块注册，注入所有 Controller 和 Service |
| `content.service.ts` | 核心编排服务：推荐排序、角色过滤、缓存管理 |
| `data-source.service.ts` | JeeSite 外部数据抓取：分页、并发、自动登录重试 |
| `ai-copy.service.ts` | AI 文案生成：DeepSeek/OpenAI 调用、提示词工程、质量校验 |
| `package-detail.service.ts` | 套餐详情抓取与解析：cheerio + 三种解析策略 |
| `copy.service.ts` | 文案 CRUD 和审核工作流 |
| `alert.service.ts` | 运营预警生成和管理 |
| `dashboard.service.ts` | 作战台和看板数据聚合 |
| `auto-login.service.ts` | JeeSite 自动登录和 Cookie 管理 |
| `daily-inventory-crawler.service.ts` | 每日库存快照抓取 |
| `mappers.ts` | Prisma DB 行与领域模型的双向映射 |
| `content.dto.ts` | 请求 DTO 定义和 class-validator 校验 |

### 数据流

推荐列表请求的完整数据流：

```
客户端 GET /packages/recommend
  → PackageController
    → ContentService.getRecommendations()
      → 检查内存缓存（TTL 5 分钟）
      → DataSourceService.loadDataset()
        → 检查数据缓存（TTL 5 分钟）
        → 获取 Cookie（AutoLoginService）
        → 分页请求 JeeSite API（默认并发 2，硬上限 4）
        → 解析并映射为 ContentPackage + SalesSnapshot
      → applyRoleFilter() 按角色过滤
      → 逐套餐计算：推广分、库存标记、运营标签、预警
      → 排序并返回
```

### 数据库

项目使用 SQLite，通过 Prisma Client 访问。**数据库结构唯一真源是 `prisma/migrations`**（VNext DB-003 治理，2026-07-27）：

- `prisma/schema.prisma` 是数据模型声明（生成 Prisma Client 类型 + 派生迁移）
- `prisma/seed-data.ts` 的 `ensureDatabaseSchema()` 只做「重放未应用的迁移」（等价 `migrate deploy`，可被 seed/ETL 脚本内联调用），不再包含手写 DDL
- 应用启动（`prisma-connect.ts`）只做只读结构自检，缺表/缺列即 fail-fast，**禁止任何运行时 DDL**

新增数据库字段的标准流程：

1. `prisma/schema.prisma` — 声明模型字段
2. 生成迁移：`npx prisma migrate diff --from-migrations prisma/migrations --to-schema-datamodel prisma/schema.prisma --shadow-database-url "file:./prisma/.drift-shadow.db" --script > prisma/migrations/<序号_名称>/migration.sql`
3. 应用：`npm run db:migrate`（CI 用 `npm run db:drift-check` 校验零漂移）
4. `apps/api/src/content/mappers.ts` — 更新 `DbPackage` 类型和双向映射函数（如涉及 ContentPackage）

### DTO 校验

所有 Controller 入参使用 class-validator 装饰器定义校验规则。`main.ts` 中注册了全局 `ValidationPipe`：

```typescript
app.useGlobalPipes(new ValidationPipe({
  whitelist: true,           // 自动剔除未声明的属性
  forbidNonWhitelisted: true, // 有多余属性时返回 400
  transform: true,            // 自动类型转换（如 string → number）
}));
```

新增接口时需要：
1. 在 `content.dto.ts` 中定义 DTO 类，添加校验装饰器
2. Controller 中使用 `@Body()` 或 `@Query()` 绑定 DTO 类型

### 外部数据抓取

`DataSourceService` 负责从 JeeSite 后台拉取套餐数据，关键设计：

- **缓存** — 5 分钟 TTL，减少外部请求
- **请求去重** — 缓存失效时的并发请求共享同一次抓取（in-flight dedup）
- **自动登录** — 检测到登录页面或 `result: 'login'` 时自动触发 `AutoLoginService` 重新登录
- **分页失败处理** — 失败页码会被记录并输出 warn 日志，失败比例超过 30% 直接抛异常，不会产出"看似正常"的不完整结果
- **超时保护** — 每个请求使用 `AbortController`，默认 8 秒超时

## 前端架构

### 页面路由

| 路径 | 视图组件 | 说明 |
|------|----------|------|
| `/` | DashboardView | 运营作战台 |
| `/recommendations` | RecommendationsView | 推荐套餐列表 |
| `/packages/:packageId` | PackageAnalysisView | 单套餐深度分析 |
| `/generate` | GenerateView | AI 文案生成 |
| `/communities` | CommunitiesView | 社群运营 |
| `/alerts` | AlertsView | 运营预警 |
| `/audit` | AuditView | 文案审核 |
| `/performance` | PerformanceView | 效果看板 |

所有视图组件使用路由懒加载（`() => import(...)`），按 chunk 分割。

### 状态管理

目前仅使用一个 Pinia store（`stores/role.ts`），管理当前运营角色。角色切换后，推荐页、预警页、作战台和社群页均会自动刷新数据。

### API 调用层

`apps/web/src/services/http-client.ts` 与 `http-client-pipeline.ts` 提供 axios 请求和响应管线；`apps/web/src/services/api/` 按业务域封装接口。前端请求 `/api/*` 路径，由 Vite 代理转发到后端。

### 构建优化

Vite 配置了手动分包策略（`rollupOptions.output.manualChunks`），将第三方库分为五个 chunk：

- `vendor-vue` — Vue / Pinia / Vue Router
- `vendor-ui` — Element Plus
- `vendor-charts` — ECharts / ZRender
- `vendor-http` — Axios / @content/shared
- `vendor-misc` — 其余 node_modules

## 测试

### 运行测试

```bash
npm run typecheck
npm run build
npm run lint:check
npm run format:check
npm run test:unit -w @content/api
npm run test:integration -w @content/api
npm run test:behavior -w @content/web
npm run test:legacy -w @content/web
npm run check:integrity
npm run db:validate
npm run db:drift-check
npm test                       # 完整测试入口，数量以当次输出为准
npm run test:watch -w @content/api  # watch 模式
```

P0-04 当前基线包括 API focused `34/34`、Web behavior `360/360` 和 Web legacy `351/351`。完整测试中的历史静态 pin 若出现失败，须按 [持续优化收束报告](docs/CONTINUOUS-OPTIMIZATION-SUMMARY.md) 的最新条目归因，不能用旧文档中的固定用例数判断结果。

### 测试结构

测试文件位于 `apps/api/test/`，使用 Vitest + Supertest：

| 测试文件 | 覆盖范围 |
|----------|----------|
| `inventory-flags.spec.ts` | 库存标记计算逻辑 |
| `inventory-flags-extended.spec.ts` | 扩展库存标记场景 |
| `promotion-rules.spec.ts` | 推广评分规则 |
| `operation-rules.spec.ts` | 运营标签和预警生成规则 |
| `jeesite-bargain-adapter.spec.ts` | JeeSite 数据映射转换 |
| `copy-generation.spec.ts` | 规则文案生成 |
| `copy-audit-rules.spec.ts` | 文案审核规则（13 个用例） |
| `ai-copy.service.spec.ts` | AI 文案服务（含降级） |
| `package-detail.service.spec.ts` | 套餐详情解析（三种策略） |
| `data-source.service.spec.ts` | 外部数据抓取和错误处理 |
| `ai-copy-config-api.spec.ts` | AI 配置接口（集成测试） |
| `content-api.spec.ts` | 推荐 + 文案生成 + 审核全链路 |
| `content-inventory-api.spec.ts` | 库存趋势 + 预警全链路 |

### 编写新测试

集成测试使用 NestJS Testing 模块创建应用实例，通过 Supertest 发送 HTTP 请求：

```typescript
import { Test } from '@nestjs/testing';
import request from 'supertest';
import { AppModule } from '../src/app.module';
import { DataSourceService } from '../src/content/data-source.service';

const moduleRef = await Test.createTestingModule({
  imports: [AppModule]
})
  .overrideProvider(DataSourceService)
  .useValue({ loadDataset: vi.fn().mockResolvedValue({ packages: [...], snapshots: [...] }) })
  .compile();

const app = moduleRef.createNestApplication();
await app.init();
const server = app.getHttpServer();

const res = await request(server).get('/api/content/packages/recommend').expect(200);
```

外部数据源通常使用 `vi.fn().mockResolvedValue()` 进行 mock，避免测试依赖真实网络请求。

## 构建与部署

### 构建产物

```bash
npm run build
```

构建顺序：shared → api（tsc 编译） → web（vue-tsc 类型检查 + vite build）。

前端构建产物输出到 `apps/web/dist/`，后端编译产物在 `apps/api/dist/`。

### Windows Desktop / EXE（当前延期）

本轮不执行 `npm run build:exe`、`npm run package:exe`、安装器生成、`win-unpacked` 验收或 EXE smoke。`apps/desktop`、`electron-builder.yml` 和 `scripts/package-exe.js` 仍是后续 Windows 发布的实现入口，但不构成当前非 EXE 验收证据。

恢复桌面发布时，必须遵守 [打包优化文档](docs/PACKAGING.md) 的安全边界：配置由用户目录管理，数据库位于 Electron `userData`，发布包不得包含真实 `.env`、Cookie、数据库或密钥；发布验收需要真实安装进程、`/ready` 和业务路由证据。

### 静态资源托管

`main.ts` 中配置了 `express.static` 托管前端构建产物，非 `/api` 和 `/health` 路径统一回落到 `index.html`，支持 Vue history 路由。桌面资源布局属于延期发布验收内容，当前仅验证 Web 构建产物本身。

## 常见问题

### 构建报错：Could not find a declaration file for module

缺少类型声明包时安装对应的 `@types/xxx`。例如 `compression` 需要 `npm i -D @types/compression -w @content/api`。

### 测试报错：column does not exist

数据库缺少新字段。运行 `npm run db:migrate` 应用最新迁移；用 `npm run db:drift-check` 确认实际库与 Schema 零漂移。默认开发库是 `prisma/dev.db`；若删除它，重新运行 `npm run prepare:db`（内部即 `migrate deploy`）即可。

### 端口被占用

`EADDRINUSE` 错误。修改 `.env` 中的 `PORT` 值，或关闭占用端口的进程。

### 前端页面 404（本地开发）

确认 `npm run dev` 已启动且 `apps/web/dist/` 或 Vite 开发服务可用；先运行 `npm run build` 检查 Web 构建。EXE 资源目录属于延期的 Windows 发布验收，不在当前开发门禁内。

### Cookie 频繁过期

配置 `EXTERNAL_API_USERNAME` 和 `EXTERNAL_API_PASSWORD`，系统会自动登录并缓存 Cookie（2 小时有效期）。详见 [自动登录文档](docs/AUTO_LOGIN.md)。

### 区域/商家角色切换后页面空白

这是预期行为。区域和商家角色需要在推荐页顶部筛选栏中选择具体的区域或商家后才会过滤数据，不再使用硬编码的测试 ID。

## 编码规范

- 后端使用 TypeScript strict 模式，所有变量需显式标注类型或使用类型推断
- DTO 必须使用 class-validator 装饰器，Controller 入参必须绑定 DTO 类型
- 共享类型定义放在 `packages/shared/src/index.ts`，前后端通过 `@content/shared` 引用
- 数据库字段新增必须更新 `schema.prisma` 并生成 `prisma/migrations` 迁移；仅在领域映射涉及套餐模型时同步 `mappers.ts`。`seed-data.ts` 不再维护手写 DDL。
- 前端组件使用 `<script setup lang="ts">`，Element Plus 组件按需自动导入
- 提交前至少运行 `npm run typecheck`、`npm run build`、相关 API/Web 回归、`npm run check:integrity` 和 `npm run db:drift-check`；完整命令见 [文档对齐总览](docs/DOCUMENTATION-STATUS.md)。
