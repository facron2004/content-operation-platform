# 开发者指南

本文档面向参与内容运营与增长中台开发的工程师，全面覆盖环境搭建、Monorepo 架构、后端 34 领域模块、前端 50+ 视图分层、金钱分精度引擎、数据库迁移与治理、测试与代码规范。

> **当前基线（2026-08-18）**：系统已完成全栈 TypeScript 类型对齐、34 个后端 NestJS 领域模块与 50+ 前端视图分层。数据库唯一结构真源为 `prisma/migrations`（已完成 0001 至 0029 号迁移），启动期执行只读结构自检；金钱精度全链路落地分整数（`BigInt` / `*Fen`）与 `MoneyView` 响应拦截器；关键写操作强制 `@RequireIdempotency` 幂等守卫与 Outbox 事务事件闭环。最新全局状态与验收证据见 [文档对齐总览](docs/DOCUMENTATION-STATUS.md)。

---

## 目录

- [环境要求](#环境要求)
- [首次搭建与启动](#首次搭建与启动)
- [Monorepo 工作区结构](#monorepo-工作区结构)
- [后端架构与核心机制](#后端架构与核心机制)
  - [1. 领域模块划分 (34 Modules)](#1-领域模块划分-34-modules)
  - [2. 金钱分精度架构 (Integer Fen)](#2-金钱分精度架构-integer-fen)
  - [3. 数据库迁移与治理规范](#3-数据库迁移与治理规范)
  - [4. 关键写操作幂等性守卫](#4-关键写操作幂等性守卫)
  - [5. 事务性 Outbox 消息引擎](#5-事务性-outbox-消息引擎)
  - [6. 外部数据源抓取与单飞缓存](#6-外部数据源抓取与单飞缓存)
- [前端架构与交互设计](#前端架构与交互设计)
  - [1. 特性分层与视图组织](#1-特性分层与视图组织)
  - [2. 样式解耦与规范](#2-样式解耦与规范)
  - [3. HTTP 请求管线与错误隔离](#3-http-请求管线与错误隔离)
  - [4. 路由导航可靠性与权限控制](#4-路由导航可靠性与权限控制)
- [测试体系与质量门禁](#测试体系与质量门禁)
- [开发规范与工作流](#开发规范与工作流)

---

## 环境要求

- **Node.js**：`>=20.0.0 <23`
- **npm**：`>=10.0.0`（统一使用 npm workspaces 管理依赖，`package-lock.json` 为唯一锁文件）
- **操作系统**：Windows 10/11 / macOS / Linux
- **Git**

---

## 首次搭建与启动

```bash
# 1. 克隆代码仓库
git clone <repo-url>
cd "Content Operation Platform"

# 2. 安装依赖并自动触发 Prisma Client 生成
npm install

# 3. 复制环境变量文件
cp .env.example .env

# 4. 初始化 SQLite 数据库与执行迁移（默认创建 prisma/dev.db）
npm run prepare:db

# 5. 启动前后端联合开发服务器
npm run dev
```

- `npm run dev` 启动统一开发服务器：
  - 后端 API：NestJS / tsx watch 运行于内部端口 **3101**。
  - 前端 Web：Vite 开发服务器运行于端口 **3100**，并通过 Vite 代理将 `/api` 请求透明转发到 3101。
- 浏览器统一访问 **`http://localhost:3100`**。

---

## Monorepo 工作区结构

项目采用 npm workspaces 进行多包管理：

```
@content/shared    packages/shared      前后端共享 TypeScript 类型、枚举与纯函数工具
@content/api       apps/api             NestJS 后端服务应用
@content/web       apps/web             Vue 3 前端 SPA 业务应用
(desktop)          apps/desktop         Electron 桌面端主进程
```

- **包构建依赖顺序**：`@content/shared` 必须先于 `@content/api` 与 `@content/web` 构建。
- 执行 `npm run build` 会自动按 `shared → api → web` 顺序执行完整编译。

---

## 后端架构与核心机制

### 1. 领域模块划分 (34 Modules)

后端采用 NestJS 标准模块化分层，核心业务域模块位于 `apps/api/src/`：

| 模块名称 | 目录路径 | 核心业务职责 |
|----------|----------|--------------|
| `content` | `src/content/` | 核心套餐推荐算法、推广评分计算、多渠道文案生成与人工审核工作流 |
| `gmv` | `src/gmv/` | GMV 聚合计算、历史日趋势预聚合（DailyMetrics）、历史数据回填与 Top 商家榜 |
| `overview` | `src/overview/` | 经营总览大盘、实时经营指标、支付渠道占比与多维运营分析 |
| `movement` | `src/movement/` | 动销与不动销分析、滞销 SKU 预警（15天/30天未销）与动销漏斗下钻 |
| `operation`| `src/operation/` | 经营中心大盘、今日运营看板、运营预警生成与处置历史 |
| `refund` | `src/refund/` | 退款/核销看板、全站单数口径退款率与核销率计算、退款审核流水分析 |
| `user-center` | `src/user-center/` | 全量会员目录快照同步（串行分页 + 原子指针切换）、用户生命周期分层与新增看板 |
| `product-center` | `src/product-center/` | 商品中心、三库存口径模型（初始/现存/当日）、状态流转与单飞同步 |
| `gap-center` | `src/gap-center/` | 组合套餐管理、商家综合健康评分（MerchantScores） |
| `order-center` | `src/order-center/` | 订单中心、交易流水审计、线上/余额支付拆分展示（只读中台边界） |
| `finance-center` | `src/finance-center/` | 财务中心、商家提货分快照同步与聚合、资产流水与对账 |
| `stores` | `src/stores/` | 合作商店铺同步、地理经纬度解析、深圳 9 大行政区归属匹配 |
| `merchant` | `src/merchant/` | 商家主数据管理、合作商资质申请与审批流 |
| `merchant-sales` | `src/merchant-sales/` | 商家销售排行、商家日指标聚合与多周期对比 |
| `distribution-task` | `src/distribution-task/` | 任务中心分发任务创建、审批、发布与生命周期管理 |
| `campaign` | `src/campaign/` | 运营活动创建、关联套餐配置与活动进度跟踪 |
| `community` | `src/community/` | 社群画像管理、社群库导入与推荐套餐匹配 |
| `marketing-private` | `src/marketing-private/` | 私域营销任务、客户线索（CRM Leads）跟进与转化分析 |
| `welfare-point` | `src/welfare-point/` | 福利点账户、发放与兑换明细管理 |
| `member-integral` | `src/member-integral/` | 会员积分流水与变动记录 |
| `audit-log` | `src/audit-log/` | 操作审计日志记录、敏感写操作轨迹追踪 |
| `user-access` | `src/user-access/` | 用户访问控制、IAM 角色与权限策略中心 |
| `auth` | `src/auth/` | 本地认证守卫、桌面环境凭证生成与 Cookie 校验 |
| `idempotency` | `src/idempotency/` | 关键写操作幂等性守卫 (`@RequireIdempotency`) 与记录存储 |
| `outbox` | `src/outbox/` | 事务性 Outbox 事件调度器、事件分发与重试任务 |
| `jobs` | `src/jobs/` | 定时任务调度、每日库存快照抓取、过期幂等记录清理 |
| `ai-copy` | `src/content/ai-copy/` | DeepSeek / OpenAI 大模型对接、提示词工程与违禁词过滤 |
| `package-detail`| `src/content/package-detail/` | 套餐详情 HTML 抓取与 Cheerio 结构化解析策略 |
| `money` | `src/money/` | 金钱分精度转换、MoneyView 拦截器与日指标重算 |
| `prisma` | `src/prisma/` | Prisma ORM 实例、双写扩展 (`moneyFenExtension`) 与只读结构自检 |
| `common` | `src/common/` | 通用常量、工具函数与全局管道配置 |
| `config` | `src/config/` | 环境变量加载、验证与默认配置管理 |
| `domain` | `src/domain/` | 核心领域规则（推广分算法、预警判定、违禁词列表） |
| `zero-sales` | `src/zero-sales/` | 零动销监控与处置时间线 |

---

### 2. 金钱分精度架构 (Integer Fen)

为杜绝 JavaScript 浮点数计算精度丢失（如 `0.1 + 0.2 = 0.30000000000000004`），系统推行整数分架构：

1. **底层存储**：所有金额字段在 SQLite 中统一以 `BigInt` 存储整数分（列名以 `*Fen` 结尾，如 `paidAmountFen`、`totalGmvFen`）。
2. **ORM 双写扩展**：`apps/api/src/prisma/money-fen-extension.ts` 通过 Prisma Client `$extends` 拦截 `create` / `update` / `upsert` / `createMany`，在写入 Float 元的同时自动将数值转为 `round(val * 100)` 填入对应 `*Fen` 字段。
3. **响应拦截器 (`MoneyViewInterceptor`)**：
   - 实体中的 `*Fen` 字段在输出时被序列化为 `string`，杜绝前端 `JSON.parse` 64 位整型溢出。
   - 自动生成对应的 `*Display` 字段（如 `paidAmountDisplay = "39.90"`），前端直接展示。
4. **对账校验**：`scripts/reconcile-money-fen.ts` 扫描 10 个核心金钱模型，校验 `storedFen === round(float * 100)`，确保差异为 0 分：
   ```bash
   npm run db:reconcile        # 对账检查
   npm run db:reconcile:fix    # 自动修复不一致
   ```

---

### 3. 数据库迁移与治理规范

数据库唯一结构真源是 **`prisma/migrations`** 目录（VNext DB-003 治理规范）：

- **禁止运行时 DDL**：应用启动（`prisma-connect.ts`）仅执行只读结构自检，若表或列缺失立即 fail-fast 报错，绝不在运行时执行 `ALTER TABLE` 或 `CREATE TABLE`。
- **新增数据库字段的标准流程**：
  1. 修改 `prisma/schema.prisma` 声明模型。
  2. 生成迁移 SQL：
     ```bash
     npx prisma migrate diff --from-migrations prisma/migrations --to-schema-datamodel prisma/schema.prisma --shadow-database-url "file:./prisma/.drift-shadow.db" --script > prisma/migrations/<序号_名称>/migration.sql
     ```
  3. 更新 `prisma/migrations/migration-policy.json` 中的 checksum。
  4. 应用迁移：`npm run db:migrate`。
  5. 校验零漂移：`npm run db:drift-check`。

---

### 4. 关键写操作幂等性守卫

关键业务写接口（任务创建/发布、活动创建、GMV 回填、社群导入）均受 `@RequireIdempotency` 装饰器保护：

```typescript
@Post('publish')
@RequireIdempotency()
async publishTask(
  @Param('id') id: string,
  @Headers('Idempotency-Key') idempotencyKey: string,
  @Body() dto: PublishTaskDto
) { ... }
```

- **校验规则**：
  - 缺失 `Idempotency-Key` 请求头返回 `400 Bad Request`。
  - 相同 Key + 相同 Payload：直接重放第一次成功响应，不重复执行写入。
  - 相同 Key + 不同 Payload：返回 `409 Conflict` 冲突错误。
- **过期清理**：后台定时任务自动清理超过保留期限的历史幂等记录。

---

### 5. 事务性 Outbox 消息引擎

为保证分布式与模块间事件解耦的数据强一致性，系统采用 Outbox 模式：

1. **事务内写入**：业务操作（如发布分发任务）与 Outbox 事件记录在同一个 Prisma 数据库事务中原子提交。
2. **异步调度分发**：`OutboxProcessorJob` 扫描未处理事件，按注册的 Handler 进行分发执行（例如触发操作审计日志写入）。
3. **指数退避重试**：执行失败自动计算 `nextRetryAt`，最多重试 5 次，超出后标记为 `failed`，杜绝消息丢失。

---

### 6. 外部数据源抓取与单飞缓存

`DataSourceService` 负责抓取 JeeSite 外部后台数据：

- **5 分钟内存缓存**：`CONTENT_CACHE_TTL_MS=300000`，减少源站负载。
- **单飞并发去重 (In-Flight Single Flight)**：当缓存过期时，多个并发请求共享同一个后台抓取 Promise，防止缓存击穿。
- **自动登录恢复**：拦截到 Session 失效或登录页重定向时，自动调用 `AutoLoginService` 重新登录并重试业务请求。
- **受控串行全量同步**：会员目录（16万+）、店铺目录（1900+）、提货分记录（1.5万+）均采用后台受控单线程串行分页，写入 staging 暂存区并在完整成功后单事务原子切换指针。

---

## 前端架构与交互设计

### 1. 特性分层与视图组织

前端代码位于 `apps/web/src/`，按特性与职责清晰分层：

- `views/`：50+ 页面视图组件，采用路由懒加载。
- `features/`：按业务域收敛的 Composables 与局部组件（如 `features/gmv/`、`features/merchant-sales/`、`features/user-center/`）。
- `components/`：通用 UI 组件（`AppleButton`、`AppleDatePicker`、`EmptyState`、`TableSkeleton`、`ErrorAlert` 等）。
- `services/`：API 客户端与请求定义（`services/api/*.api.ts`）。
- `stores/`：Pinia 状态管理（当前运营角色与全局上下文）。

---

### 2. 样式解耦与规范

为保持单文件组件（SFC）精简，大型视图与通用组件的 CSS 已抽离至独立样式文件：

- 组件样式：`apps/web/src/styles/components/`（如 `apple-button.css`、`apple-date-range-picker.css`、`gmv-kpi-row.css`）。
- 页面样式：`apps/web/src/styles/views/`（如 `order-center.css`、`user-center.css`、`merchant-sales.css`）。
- 支持暗色模式主题切换：`apps/web/src/styles/dark-theme.css`。

---

### 3. HTTP 请求管线与错误隔离

- **请求管线 (`http-client-pipeline.ts`)**：统一处理超时、401 登录跳转、500 错误提示与取消标记。
- **错误状态隔离**：复合页面（如商家销售、退款核销、GMV 驾驶舱）将多个子模块的错误状态独立拆分（如 `kpiError`、`trendError`、`rankingError`），某个子图表失败不影响其余区域展示，并提供局部重试。
- **组件生命周期守卫 (`isActive`)**：异步请求返回时若组件已卸载或抽屉已关闭，主动丢弃迟到响应，避免回写已销毁视图。

---

### 4. 路由导航可靠性与权限控制

- **动态加载容错 (`withImportRetry`)**：前端视图路由懒加载具备预加载失败软重试机制，防止网络抖动导致的页面白屏。
- **细粒度权限控制 (`route-permissions.ts`)**：路由守卫根据用户当前角色（平台/区域/商家/审核/管理员）动态校验页面与操作权限。

---

## 测试体系与质量门禁

系统拥有完备的自动化测试体系：

```bash
# 全栈类型与完整性检查
npm run typecheck                # TypeScript 类型检查 (shared + api + web)
npm run check:integrity          # 源码导入完整性校验 (1221 文件, 0 未解析导入)
npm run db:validate              # Prisma 模型声明校验
npm run lint:check               # ESLint 静态代码检查

# 自动化测试执行
npm run test:unit -w @content/api        # 后端单元测试
npm run test:integration -w @content/api # 后端集成测试
npm run test:behavior -w @content/web    # 前端行为回归测试
npm run test:legacy -w @content/web      # 前端兼容测试
npm run test:migration-policy            # 数据库迁移策略与 Checksum 校验

# 完整测试套件
npm test
```

---

## 开发规范与工作流

### 1. Git 提交信息规范 (Commitlint)

遵循 Conventional Commits 规范，由 Husky + Commitlint 自动化校验：

- `feat`: 新增功能
- `fix`: 修复缺陷
- `refactor`: 重构代码（不改变外部行为）
- `chore`: 依赖更新、构建配置调整
- `docs`: 文档变动
- `test`: 增加或修改测试用例

### 2. 代码格式化规范 (Prettier & ESLint)

- 提交前通过 `lint-staged` 自动运行 Prettier 与 ESLint 修复。
- 手动格式化：`npm run format`。
