# Cap parallelism + heap: type-aware ESLint loads the TS program per process
# (~1.8GB); with 16 CPUs the default would exhaust RAM on large commits.
NODE_OPTIONS=--max-old-space-size=4096 npx lint-staged --concurrent 3
